.announcement-bar {
  text-align: center;
  padding: 10px; }

.grid {
  display: grid;
  grid-template-columns: 20% 60% 20%;
  align-items: center; }

hr {
  clear: both;
  border-top: solid #d7d8d2;
  border-width: 1px 0 0;
  margin: 34px 0 0;
  height: 0; }
  hr.hr--border {
    margin: 0; }

input[type=text]::placeholder {
  font-size: 16px; }

.search-bar {
  display: inline-flex;
  gap: 8px; }
  .search-bar .search-form {
    display: flex;
    align-items: center;
    width: 100%; }
    .search-bar .search-form--search-text {
      padding: 8px;
      background: transparent;
      border: none;
      font-weight: 400;
      width: 100%; }
    .search-bar .search-form--submit {
      padding: 0;
      background: transparent;
      border: none;
      cursor: pointer; }
      .search-bar .search-form--submit img {
        width: 20px; }

.navigation ul {
  list-style: none;
  display: flex;
  gap: 20px;
  padding: 0;
  margin: 0 auto; }

.mini-cart--link {
  display: inline-flex;
  gap: 10px;
  text-decoration: none; }
  .mini-cart--link img {
    width: 20px; }

.top-bar {
  padding-top: 7.5px;
  padding-bottom: 7.5px;
  font-size: 17.1px; }

.menu-link--item {
  text-decoration: none; }

.homepage-navigation--menu {
  list-style: none;
  display: flex;
  flex-direction: column;
  padding: 0; }

.navigation-link {
  display: inline-block;
  padding: 15px 0;
  border-bottom: 1px solid #d7d8d2; }
  .navigation-link:first-child {
    border-top: 1px solid #d7d8d2; }
  .navigation-link--item {
    text-decoration: none;
    color: #333; }
    .navigation-link--item.current {
      font-weight: bold;
      color: #000; }

.homepage-menu--icon {
  visibility: hidden; }

@media (max-width: 750px) {
  .navigation ul {
    display: none; }
  .search-form--search-text {
    display: none; }
  .small--hide {
    display: none; }
  .mini-cart--link img {
    width: 30px; }
  .search-form--submit img {
    width: 30px; }
  .header-bottom--img {
    display: flex;
    align-items: center;
    justify-content: center; }
  .hr--border {
    display: none; }
  .homepage-content--grid {
    flex-direction: column; }
  .nav1 {
    width: 100%; }
  .nav2 {
    width: 100%; }
  .homepage-menu--icon {
    visibility: visible;
    text-align: center;
    font-size: 33.3px;
    cursor: pointer; }
  .homepage-navigation {
    padding: 15px 0;
    border-top: 1px solid #d7d8d2;
    border-bottom: 1px solid #d7d8d2; }
    .homepage-navigation--menu {
      display: none;
      transition: height 0.3s ease; }
      .homepage-navigation--menu.active {
        display: inline-flex;
        height: auto;
        text-align: center;
        width: 100%; }
  .navigation-link {
    text-align: center;
    border-bottom: 0px; }
    .navigation-link:first-child {
      border-top: 0px; } }

.homepage-navigation--menu {
  list-style: none;
  display: flex;
  flex-direction: column;
  padding: 0;
  text-align: center; }

.navigation-link {
  border-bottom: 1px solid #d7d8d2;
  text-align: left;
  padding: 15px 0; }
  .navigation-link:first-child {
    border-top: 1px solid #d7d8d2; }
  .navigation-link--item.current {
    font-weight: bold;
    color: #000; }

@media (max-width: 750px) {
  .homepage-navigation {
    padding: 15px 0;
    border-top: 1px solid #d7d8d2;
    border-bottom: 1px solid #d7d8d2; }
    .homepage-navigation--menu {
      display: none;
      transition: height 0.3s ease;
      text-align: center; }
      .homepage-navigation--menu.active {
        display: inline-flex;
        height: auto;
        text-align: center;
        width: 100%;
        text-align: center; }
  .navigation-link {
    text-align: center;
    border-bottom: 0px;
    padding: 15px 0; }
    .navigation-link:first-child {
      border-top: 0px; } }

.collection-item {
  position: relative;
  opacity: .8; }
  .collection-item:hover {
    opacity: .45; }
  .collection-item--img {
    width: 100%;
    height: 100%; }
  .collection-item--title {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    font-size: 30px;
    font-weight: 400;
    text-align: center;
    width: 100%;
    color: #fff; }

@media (max-width: 750px) {
  .collection-grid {
    display: grid; }
  .collection-item--title {
    font-size: 16px; } }

.contact-us .container {
  max-width: 800px; }

.breadcrumb-nav {
  margin-bottom: 20px; }

.breadcrumb-list {
  display: flex;
  list-style: none;
  padding: 0;
  margin: 15px 0; }

.breadcrumb-item {
  margin-right: 10px; }
  .breadcrumb-item a {
    text-decoration: none;
    color: #007bff; }
    .breadcrumb-item a:hover {
      text-decoration: underline; }
  .breadcrumb-item.active {
    color: #555; }

.title-and-description {
  margin-bottom: 20px; }

.contact-info {
  margin-bottom: 20px; }
  .contact-info ul {
    list-style: none;
    padding: 0; }
    .contact-info ul li {
      margin-bottom: 5px; }

.contact-form {
  display: flex;
  flex-direction: column; }

.form-blocks {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  flex-direction: column; }

.form-block {
  width: 300px !important;
  margin-bottom: 20px !important; }
  .form-block label {
    display: block;
    margin-bottom: 5px; }
  .form-block input, .form-block textarea {
    width: 100%;
    padding: 10px;
    font-size: 16px;
    max-width: 300px;
    border: 1px solid #ccc; }
  .form-block textarea {
    resize: vertical;
    height: 300px; }

.btn {
  padding: 10px 20px !important;
  font-size: 16px;
  background-color: red;
  color: #fff;
  border: none;
  cursor: pointer;
  max-width: 320px !important;
  text-transform: uppercase; }

@media (max-width: 750px) {
  .form-block {
    width: 100% !important; }
  .btn {
    max-width: 100% !important; }
  .form-block input, .form-block textarea {
    max-width: 100%  !important; } }

.copyright-content {
  display: flex;
  justify-content: space-around;
  align-items: center;
  flex-wrap: wrap; }

.copyright-text {
  flex: 1 1 50%;
  box-sizing: border-box; }

.visa-images {
  flex: 1 1 50%;
  display: flex;
  align-items: center;
  box-sizing: border-box; }
  .visa-images img {
    max-width: 50px;
    margin: 10px; }

@media (max-width: 750px) {
  .copyright-text {
    text-align: center; }
  .visa-images {
    justify-content: center; } }

.contactus-section {
  display: flex;
  justify-content: center;
  align-items: center; }

.contactus-content {
  display: flex;
  width: 100%;
  margin: 0 auto; }

.contactus-image {
  width: 100%;
  max-width: 100%;
  height: 100%;
  object-fit: cover;
  display: block; }
  .contactus-image-wrapper {
    flex: 0 1 50%;
    margin-right: 20px; }

.contactus-info {
  flex: 0 1 50%; }

.contactus-heading {
  font-size: 24px;
  margin-bottom: 10px; }

.contactus-text {
  font-size: 16px;
  line-height: 1.6;
  color: #666; }

@media (max-width: 750px) {
  .contactus-heading {
    text-align: center; }
  .contactus-content {
    flex-direction: column; }
  .contactus-image-wrapper {
    margin-right: 0px; }
  .feature-row__image--content {
    padding: 20px 0 !important; } }

.connect-content {
  margin-top: 10px !important; }

.connect-heading {
  text-decoration: underline !important; }

.footer-container {
  display: flex;
  justify-content: space-between; }

.footer-column {
  flex: 1 0 25%;
  min-width: 250px;
  box-sizing: border-box; }
  .footer-column h4 {
    font-weight: bold;
    margin-bottom: 10px; }
  .footer-column ul, .footer-column p, .footer-column form {
    margin: 0;
    padding: 0;
    list-style: none; }
  .footer-column a {
    text-decoration: none;
    color: #333; }
  .footer-column form input[type='email'] {
    width: 70%;
    outline: none;
    border: 0;
    height: 48px; }
  .footer-column form button {
    padding: 10px;
    cursor: pointer;
    border: 0;
    outline: none;
    background: #c13121;
    color: #fff;
    font-size: 15px;
    text-transform: uppercase; }

.know-form {
  display: flex; }
  .know-form .btn {
    padding: 10px;
    cursor: pointer;
    border: 0;
    outline: none;
    background: #c13121;
    color: #fff;
    outline: none;
    height: 48px;
    font-size: 15px;
    text-transform: uppercase; }

.know-heading {
  margin-bottom: 10px !important; }

@media (max-width: 750px) {
  .footer-container {
    flex-direction: column; } }

.feature-row__item {
  display: flex;
  align-items: center; }

.feature-row__image-wrapper {
  flex: 0 1 50%; }

.feature-row__image--content {
  padding: 20px;
  flex: 0 1 50%; }

.feature-row__image--title {
  font-size: 20px;
  font-weight: bold; }

.feature-row__image--img {
  width: 100%;
  height: 100%;
  object-fit: cover; }

.feature-row__image--text {
  margin: 0; }

@media (max-width: 750px) {
  .feature-row__item {
    flex-direction: column !important; }
  .feature-row__image-wrapper {
    width: 100%; }
  .feature-row__image--content {
    width: 100%;
    text-align: center; } }

.slide {
  text-align: center;
  position: relative; }

.slideshow-container {
  width: 100%;
  margin: auto;
  position: relative; }

.collection-content {
  position: absolute;
  top: 50%;
  width: 100%; }
  .collection-content--link {
    font-size: 24px;
    margin-bottom: 10px;
    color: #fff;
    text-decoration: none;
    font-size: 20px;
    padding: 10px;
    background-color: #c13121; }

.collection-link {
  display: block; }

.collection-image {
  width: 100%;
  height: 500px;
  object-fit: cover;
  margin: 0 auto; }

.slider-controls {
  text-align: center;
  margin-top: 10px; }
  .slider-controls button {
    background: none;
    border: none;
    font-size: 18px;
    cursor: pointer; }

.slider-dots {
  display: inline-block;
  margin: 0 10px; }

.slider-item {
  position: relative;
  display: flex;
  width: 100% !important;
  justify-content: center;
  align-items: center;
  margin-top: 10px; }
  .slider-item--controls {
    right: 4px;
    position: absolute; }
  .slider-item--dots {
    display: flex; }

.slider-pause {
  background: #fff;
  border: navajowhite;
  color: #c13121; }

.homepage-slider--img {
  width: 100%; }

.slick-dots {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
  padding: 0; }
  .slick-dots li {
    margin: 0 15px;
    color: #c13121;
    font-size: 25px; }
    .slick-dots li button {
      display: none; }

.slick-active {
  color: #000 !important; }

.prev {
  margin: 0 27px;
  background: transparent;
  border: none;
  color: #c13121;
  cursor: pointer; }

.next {
  background: transparent;
  border: none;
  color: #c13121;
  cursor: pointer; }

.subscribe-form .btn {
  cursor: pointer;
  flex: 1 1 20%;
  max-width: 130px !important;
  color: #fff;
  height: 48px;
  text-transform: uppercase;
  background-color: #c13121;
  font-size: 15px;
  border: none; }

.subscribe-section {
  background-color: #f8f8f8;
  text-align: center; }

.subscribe-content {
  max-width: 600px; }

.subscribe-form {
  display: flex;
  align-items: center;
  flex-wrap: wrap; }
  .subscribe-form input[type='email'] {
    height: 48px;
    flex: 1 1 70%;
    max-width: 400px; }
  .subscribe-form button {
    padding: 10px 20px;
    cursor: pointer;
    flex: 1 1 20%;
    max-width: 130px !important;
    color: #fff;
    text-transform: uppercase;
    background-color: #c13121;
    font-size: 15px; }
  .subscribe-form input[type='email'], .subscribe-form button {
    border: none;
    height: 48px;
    outline: none; }

@media (max-width: 750px) {
  .subscribe-form .btn {
    max-width: 100% !important; } }
